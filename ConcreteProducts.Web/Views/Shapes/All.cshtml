@model IEnumerable<ShapeListingViewModel>

@{
    ViewData["Title"] = "All Shapes";
}

<div class="container">
    <div class="row">
        <h2 class="headingMargin text-center offset-md-3 col-md-6 col-sm-12">@ViewData["Title"]</h2>
        <table class="table table-hover offset-md-1 col-md-10">
            <thead>
                <tr>
                    <th class="col-md-1 text-center" scope="col">#</th>
                    <th class="col-md-2 text-center" scope="col">Shape name</th>
                    <th class="col-md-2 text-center" scope="col">Shape dimensions</th>
                    <th class="col-md-2 text-center" scope="col">Warehouse name</th>
                    <th class="col-md-3 text-center" scope="col"><i class="fas fa-edit"></i> Edit/ <i class="fas fa-times"></i> Delete</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var shape in Model)
                {
                    <tr>
                        <th scope="row" class="text-center">@shape.Id</th>
                        <td class="text-center">@shape.Name</td>
                        <td class="text-center">@shape.Dimensions</td>
                        <td class="text-center">@shape.WarehouseName</td>
                        <td class="text-center">
                            <div class="input-group-append">
                                <a class="btn btn-info offset-3 col-3 text-center" title="Edit" asp-area="" asp-controller="Shapes" asp-action="Edit" asp-route-id="@shape.Id"><i class="bi bi-pencil-square"></i></a>
                                <a class="btn btn-danger offset-3 col-3 text-center" title="Delete" asp-area="" asp-controller="Shapes" asp-action="Delete" asp-route-id="@shape.Id"><i class="bi bi-x-lg"></i></a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
        <a class="btn btn-success text-center offset-md-3 col-md-6 col-sm-12" title="Add new" asp-area="" asp-controller="Shapes" asp-action="Add"><i class="bi bi-plus-circle"></i> Add new</a>
    </div>
</div>


